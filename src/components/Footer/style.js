import styled from "styled-components"

export const Container = styled.footer`
    color: var(--bege);
    border-radius: 32px;

    display: flex;
    justify-content: center;
    align-items: center;

    padding: 16px;
    margin-bottom: 32px;
`